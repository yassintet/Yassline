{
  "name": "yassline",
  "version": "1.0.0",
  "description": "Yassline Tour Platform",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "postinstall": "cd backend && npm install --production"
  },
  "engines": {
    "node": ">=18.x"
  }
}
